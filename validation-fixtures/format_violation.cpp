// FIXTURE: trips clang-format. Copy into src/ on a throwaway branch.
// Intentionally mis-formatted: wrong indent, spacing, brace placement.
#include "app/calculator.hpp"
namespace app{
int   badlyFormatted(int   a,int b){
        if(a>b){return a ;}
  return b;}
}
